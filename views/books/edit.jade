extends ../layout 

block content 

  div.container
    h1 Editing #{book.title}
    form(action='/books/#{book.id}', method='post')
      div.form-group
        label(for="author") Author
        input.form-control(name="author" type="text" id="author" value= book.author placeholder="Author")
      div.form-group
        label(for="title") Title
        input.form-control(name="title" type="text" id="title" value= book.title placeholder="Title")
      div.form-group
          label Rate this book
          p
            input(type="radio" name="rating" value="1" checked=book.rating===1)
            one 1 (Lame)
      div.form-group
        input(type="radio" name="rating" value="2" checked=book.rating===2)
        two 2
      div.form-group
        input(type="radio" name="rating" value="3" checked=book.rating===3)
        three 3
      div.form-group
        input(type="radio" name="rating" value="4" checked=book.rating===4)
        four 4 (Awesome)
      div.form-group
        label(for="description") Description
        textarea.form-control(name="description" rows="5" id='description') #{book.description}
      button.btn.btn-primary(type="submit") Update

                
  
